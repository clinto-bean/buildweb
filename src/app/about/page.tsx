import React from "react"

type Props = {}

function page({}: Props) {
  return (
    <section id='about' className='flex justify-center items-center size-full'>
      hi
    </section>
  )
}

export default page
